// querry selector
let firstEle = document.querySelector("p"); //when we needed first elem
console.dir(firstEle);

let allfirstEl = document.querySelectorAll(".heading"); //all elem
console.dir(allfirstEl);